info:
  name: List all users
  type: http
  seq: 16

http:
  method: GET
  url: http://127.0.0.1:8000/users/get_all
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
