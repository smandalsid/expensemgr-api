info:
  name: get exchange rate
  type: http
  seq: 14

http:
  method: GET
  url: http://127.0.0.1:8000/exchange_rate
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
